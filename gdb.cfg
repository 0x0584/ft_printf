# global gdb configurations
set disassembly-flavor intel
set print pretty on
set logging on
set print array on
shell clear

# executable
file ft_printf

# defined commands
define valg
  shell valgrind --log-file="log-$(date +'%T').txt" --leak-check=full --show-reachable=yes --show-leak-kinds=all ./ft_printf
  shell less $(ls log-* | sort -r | head -1)
end

define ls
  shell ls --color=auto --classify ; echo ""
end

define reload
  shell make re
  file ft_printf
  shell echo "executable is loaded."
  run
end

b buffer.c:81
b buffer.c:89
